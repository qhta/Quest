<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Quest.Views"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:reflection="clr-namespace:Syncfusion.Reflection;assembly=Syncfusion.Shared.Base"
             xmlns:quest="clr-namespace:Quest;assembly=QuestIMP"
             x:Class="Quest.Views.ExcelView"
             d:DataContext="{d:DesignInstance quest:WorkbookInfoVM}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <TextBox Text="{Binding ProjectTitle}"/>
      <ListView 
      x:Name="WorksheetListView"
      Grid.Row="1"
      ItemsSource="{Binding Worksheets}" 
      HorizontalAlignment="Stretch" 
      VerticalAlignment="Stretch"
      SelectionChanged="WorksheetListView_OnSelectionChanged"
      >
        <ListView.View>
          <GridView>
            <GridViewColumn Header="Worksheet" 
                            DisplayMemberBinding="{Binding Name}" />
            <GridViewColumn Header="Quest">
              <GridViewColumn.CellTemplate>
                <DataTemplate DataType="{x:Type quest:WorksheetInfoVM}">
                  <TextBlock Text="{Binding QuestRange}"
                        MouseLeftButtonDown="RangeTextBlock_OnMouseLeftButtonDown"/>
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
            <GridViewColumn Header="Weights">
              <GridViewColumn.CellTemplate>
                <DataTemplate DataType="{x:Type quest:WorksheetInfoVM}">
                  <TextBlock Text="{Binding WeightsRange}"
                           MouseLeftButtonDown="RangeTextBlock_OnMouseLeftButtonDown"/>
                </DataTemplate>
              </GridViewColumn.CellTemplate>
            </GridViewColumn>
          </GridView>
        </ListView.View>
      </ListView>
    </Grid>
    <Grid Grid.Column="1">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
      </Grid.RowDefinitions>
      <!--<syncfusion:SfSpreadsheetRibbon 
      Grid.Row="0"
      DataContext= "{Binding ElementName=spreadsheet}" 
      />-->
      <syncfusion:SfSpreadsheet 
      x:Name="SpreadsheetControl"
      ShowSheetTabs="False"
      FormulaBarVisibility="Collapsed"
      Grid.Row="1" />
    </Grid>
  </Grid>
</UserControl>
